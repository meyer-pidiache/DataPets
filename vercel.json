{
    "builds": [{
        "src": "DataPets/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "DataPets/wsgi.py"
        }
    ]
}
